node_modules
# Worker 打包产物(由 build.mjs 生成)
dist/
public/_worker.js
public/_worker.js.map
# 本地 wrangler 状态与密钥
.wrangler
.dev.vars
scripts/seed-superadmin.sql
scripts/seed-content.sql

# 注意:以下生成产物【需要提交】,Git 构建只跑 esbuild 打包,不重新导出:
#   src/generated/mc-blocks.js   (方块元数据)
#   public/vendor/mc/**          (原版纹理)
# 提交前先确保它们已由 node scripts/export-mc-assets.mjs 生成。
/.playwright-mcp
