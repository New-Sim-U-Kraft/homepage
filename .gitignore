# 依赖
node_modules/
.pnpm-store/

# 构建产物
.nuxt/
.output/
dist/
.nitro/
.cache/

# Cloudflare
.wrangler/
.dev.vars
.dev.vars.*
!.dev.vars.example

# 环境变量
.env
.env.*
!.env.example

# License 密钥。公钥本身不敏感，但不该混在仓库根目录里
license-public.pem

# 编辑器
.idea/
.vscode/
*.swp
*.swo

# 系统
.DS_Store
Thumbs.db

# 日志与调试
*.log
npm-debug.log*
pnpm-debug.log*
.dbg/
debug-*.md

# 数据文件（历史遗留，不再入库）
data/

# 注意：public/mc/ 的图集产物**是入库的**（约 240KB）。
# 它由 scripts/build-atlas.mjs 生成，但生成依赖 sharp + minecraft-assets 两个重包，
# 放进 CI 会让每次自动部署都白装几十 MB。产物本身很小，直接提交更划算。
# 更新 Minecraft 版本时手动跑 `pnpm atlas` 重新生成并提交。
public/vendor/mc/
app/public/vendor/mc/
.playwright-mcp/
